# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'Hunger Swift' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Hunger Swift

# pod 'AERecord', '~> 3.0'

  target 'Hunger SwiftTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Hunger SwiftUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
